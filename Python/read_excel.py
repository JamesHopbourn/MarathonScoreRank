import pandas as pd

# 表格数据转为字典
sheet = pd.read_excel('data.xlsx')
data = sheet.to_dict(orient="records")

command = []
for item in data:
    if(str(item['personal_bib']).endswith("6")):
        command.append((f"{item['team_name'].rstrip()}", f"{item['personal_bib']:04}",
                   f"{item['personal_name']}", f"{item['gender']}", f"{item['record_time']}"))

print("INSERT INTO data (`team_name`,`personal_bib`,`personal_name`, `gender`, `record_time`) VALUES ")


































































































































































































































